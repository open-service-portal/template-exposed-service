# template-exposed-service
Crossplane Configuration: ExposedService — give an in-cluster Service an HTTPS URL via Gateway API (HTTPRoute + shared Gateway wildcard TLS + External-DNS). Composable expose-with-DNS+TLS building block.
